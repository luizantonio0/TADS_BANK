server:
  port: 2563

spring:
  application:
    name: gerente
  rabbitmq:
    template:
      reply-timeout: 1000000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/ms_gerente
    username: admin
    password: password
    driver-class-name: org.postgresql.Driver #
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE